import { Menu } from '@mui/material';

export const Admin = (props) => {
    return (
        <Menu
        {...props}
      >
        Hello from Menu Layer

      </Menu>
    )
}

export default Admin;